import 'package:flutter/cupertino.dart';

// Color Palette and all colors used in the APP, should be declared here

class AppColors {
  AppColors._();

  static const teal = Color(0xff02A9A2);
  static const green = Color(0xffABE562);
  static const blue = Color(0xff72ADF2);
  static const white = Color(0xffFFFFFF);
  static const black = Color(0xff000000);
  static const blackDeep = Color(0xff262E36);
  static const blackAccent = Color(0xff41484E);
  static const red = Color(0xffFF414E);
}
